branches = pi branch_name -> branch_name_b (loan ⨝ sigma customer_name = 'Smith'(borrower))
pi customer_name sigma a.branch_name = branch_name_b and customer_name ≠ 'Smith' (rho a (depositor ⨝ account) x branches)